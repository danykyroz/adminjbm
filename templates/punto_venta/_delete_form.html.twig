<form method="post" action="{{ path('punto_venta_delete', {'id': punto_ventum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ punto_ventum.id) }}">
    <button class="btn">Delete</button>
</form>
