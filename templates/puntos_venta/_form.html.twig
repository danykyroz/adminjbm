<div class="row">
    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
        <div class="card">

        <div class="header">
            <h2>
                <strong>Formulario Punto Venta</strong>
            </h2>
            
        </div>
    <div class="card-body table-responsive">

{{ form_start(form) }}
    {{ form_widget(form) }}
    <br/>
    <button class="btn btn-success">{{ button_label|default('Guardar') }}</button>
{{ form_end(form) }}
{% if(eliminar) %}
<br/>
    {{ include('puntos_venta/_delete_form.html.twig') }}

{% endif%}
</div>
</div>
</div>
</div>