<form method="post" action="{{ path('puntos_venta_delete', {'id': puntos_ventum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ puntos_ventum.id) }}">
    <button class="btn btn-danger">Eliminar</button>
</form>
