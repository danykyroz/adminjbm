{% extends 'base.html.twig' %}

{% block title %}PuntosVenta{% endblock %}

{% block body %}
    <h1>PuntosVenta</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ puntos_ventum.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ puntos_ventum.nombre }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ puntos_ventum.createdAt ? puntos_ventum.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ puntos_ventum.updatedAt ? puntos_ventum.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('puntos_venta_index') }}">back to list</a>

    <a href="{{ path('puntos_venta_edit', {'id': puntos_ventum.id}) }}">edit</a>

    {{ include('puntos_venta/_delete_form.html.twig') }}
{% endblock %}
